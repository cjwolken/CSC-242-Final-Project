#include <iostream>
#include <fstream>
#include <string>

using namespace std;

/**
Reads input file, checks against /usr/share/dict/words, and returns any words not found. 
@param input_file - the input stream

*/

void process_words(ifstream& input_file) // Needs work
{
	string word;
	input_file >> word;

	if (input_file.fail()) { return; } // Check for failure after each input
	
}
int main()
{
		ifstream input_file;
		input_file.open("testwords.txt");
		if (input_file.fail()) { return 0; } // Check for failure after opening
